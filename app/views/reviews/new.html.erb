<% provide(:title, 'Write Review') %>
<% provide(:button_text, 'Submit review') %>
<div class="container">
  <div class="col-md-8">
    <h1><%= @review.drink.name.capitalize %></h1>
  </div>
</div>

<%= form_for([@review.drink, @review]) do |f| %>

  <%= render 'layouts/errors', object: f.object %>

  <%= f.label :content %>
  <%= f.text_area :content, placeholder: "Compose your review...", class: 'form-control' %>

  <%= f.label :status %>
  <%= f.select :status, ([" ", "Helpful", "Not Helpful"]), class: 'form-control' %>

  <%= f.hidden_field :drink_id, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>